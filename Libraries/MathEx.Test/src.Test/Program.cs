namespace MathEx.Test
{
	public static class Program
	{
		public static int Main(string[] args)
		{
			/*
			spline_curve<float, CubicBezierSpline<float>> c = new spline_curve<float, CubicBezierSpline<float>>();
			c.value(0);

			plane p = new plane(new vec3(0, 5, 0), new vec3(0, 1, 0));
			vec3 p3 = p.project(vec2.zero, new vec3(0, 5, 0), new vec3[] { new vec3(0, 0, 1), new vec3(1, 0, 0) });
			vec2 p2 = p.project(vec3.zero, new vec3(1, 5, 0), new vec3[] { new vec3(0, 0, 1), new vec3(1, 0, 0) });

			foreach (var i in vec3i.zero.Line(new vec3i(20, 0, 10))) {
				Console.WriteLine(i);
			}
			*/

			return 0;
		}
	}
}
